"use strict";
// Testing spams OpenTDB API and starts unused sessions. This class will be omitted from Jest testing for now.
test("Tests outputs of Session", function () {
    expect(true).toEqual(true);
});
