import { QuestionOptions } from "../Typings/interfaces";
import { CategoryResolvable } from "../Typings/types";
import getQuestions from "./getQuestions";

test("Tests outputs of getQuestions()", () => {
  // expect(getQuestions('' as unknown as QuestionOptions)).rejects.toThrow(Error);
  // expect(getQuestions(1 as unknown as QuestionOptions)).rejects.toThrow(Error);
  // expect(getQuestions(true as unknown as QuestionOptions)).rejects.toThrow(Error);
  // expect(getQuestions().then(res => typeof res)).toBe('object');
});
